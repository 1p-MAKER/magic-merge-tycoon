import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.magicmerge.tycoon',
  appName: 'Magic Merge Tycoon',
  webDir: 'dist'
};

export default config;
